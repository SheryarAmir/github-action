# deploy-github-actions

 In this workflow we have defined a workflow that will be triggered when we push to the main branch and it will deploy our application to the server via ssh and we have also defined the docker compose file that will be used to build and run our application on the server.
